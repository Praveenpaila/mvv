## WatchMan (Vite)
# Optional: override backend base URL for watchman endpoints.
# Defaults to "/api/watchman" (and Vite dev proxy forwards to http://localhost:3000).
# If you run a separate security backend, set:
# VITE_WATCHMAN_API_URL="http://localhost:5005/watchman"
